"minitar" is a minimal example of a program that uses libarchive to
read/write various archive formats.  It's a more ambitious version of
'untar.c' that includes compile-time options to enable/disable various
features, including non-tar formats, archive creation, and automatic
decompression support.

I use this as a test bed to check for "link pollution," ensuring that
a program using libarchive does not pull in unnecessary code.

The "minitar" program is also a good starting point for anyone who
wants to use libarchive for their own purposes, as it demonstrates
basic usage of the library.

Additional example: `no_compress_examples.c`
------------------------------------------------------------
This standalone sample shows how to pack or unpack one or more paths
into a ustar archive **without** enabling gzip/bzip2 or other filters.
Build it with only the core libarchive static/shared library:

    cc -D_FILE_OFFSET_BITS=64 -o no_compress_examples \
       no_compress_examples.c -larchive

Usage:

    ./no_compress_examples c output.tar <path...>
    ./no_compress_examples x archive.tar <destination-dir>

主要逻辑与 API 说明
------------------------------------------------------------
- 打包（`pack_paths`）：
  - `archive_write_new` 创建写句柄，`archive_write_add_filter_none` 明确不启用 gzip/bzip2 等过滤器，`archive_write_set_format_ustar` 设定 tar 格式。
  - 对每个输入路径使用 `archive_read_disk_new`/`archive_read_disk_open` 遍历目录树，`archive_read_next_header2` 逐个生成 `archive_entry`，`archive_read_disk_descend` 实现递归。
  - `archive_write_header` 写 entry 元数据，常规文件再通过 `archive_write_data` 把 `read` 出的内容写入归档。

- 解包（`extract_to`）：
  - `archive_read_new` + `archive_read_support_filter_none` + `archive_read_support_format_tar` 只开启纯 tar 读取。
  - `archive_write_disk_new` 负责落盘；`archive_write_disk_set_options` 应用时间/权限/ACL/flags，`archive_write_disk_set_standard_lookup` 查找 uid/gid/用户/组名。
  - 读取循环用 `archive_read_next_header` 获取 entry，重写路径后调用 `archive_write_header` 创建目标文件（夹），有数据的 entry 通过 `copy_data` 调用 `archive_read_data_block`/`archive_write_data_block` 逐块写入。

以上 API 即构成最小的“无压缩 ustar 打包与解包”流程，可作为自定义工具的基础骨架。

Additional example: `posix_attr_examples.c`
------------------------------------------------------------
与 `no_compress_examples.c` 相同，输出为未压缩的 ustar tar 包，但打包阶段的
文件/目录属性完全通过 POSIX API 采集：

- 使用 `lstat` 读取 `st_mode`/`st_uid`/`st_gid`/`st_size`/`st_mtime` 等元
  数据，并写入 `archive_entry_set_*` 字段。
- 目录通过 `opendir`/`readdir` 递归遍历；符号链接使用 `readlink` 捕获目标
  路径，再写入 `archive_entry_copy_symlink`。
- 仅开启 `archive_write_add_filter_none` 与 `archive_write_set_format_ustar`，
  无需 zlib/bzip2 依赖。

编译：

    cc -D_FILE_OFFSET_BITS=64 -o posix_attr_examples posix_attr_examples.c \
       -larchive

用法：

    ./posix_attr_examples c output.tar <path...>
    ./posix_attr_examples x archive.tar <destination-dir>

Additional example: `untar_example.c`
------------------------------------------------------------
仅演示“解压 tar 到指定目录”的最小流程，关闭 gzip/bzip2 等过滤器，适合
直接参考或嵌入到自己的工具中。

编译：

    cc -D_FILE_OFFSET_BITS=64 -o untar_example untar_example.c -larchive

用法：

    ./untar_example archive.tar <destination-dir>
